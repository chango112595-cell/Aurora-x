# Aurora-X Ubuntu/Debian System Requirements
# Install with: sudo apt-get install -y $(cat requirements-ubuntu.txt)

# Core System Packages
python3
python3-pip
python3-venv
python3-dev

# Node.js (via NodeSource)
nodejs
npm

# Build Tools
build-essential
gcc
g++
make
cmake

# Database Support
postgresql
postgresql-client
libpq-dev
sqlite3
libsqlite3-dev

# Network & HTTP
curl
wget
ca-certificates
gnupg
lsb-release

# Process Management
supervisor
systemd

# Security
openssl
libssl-dev

# Compression
zip
unzip
gzip
tar

# Text Processing
jq
sed
awk

# Git
git

# Optional: Docker Support
docker.io
docker-compose

# Optional: Performance Monitoring
htop
iotop

# Optional: Development Tools
vim
nano
tmux
