services:
  aurora:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: ghcr.io/OWNER/REPO:latest
    ports:
      - "8000:8000"
    environment:
      - AURORA_T08_ENABLED=1
      - AURORA_UI_OK=90
      - AURORA_UI_WARN=60
    restart: unless-stopped
