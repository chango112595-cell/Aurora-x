1. PowerShell Script (Windows) — aurora_port_scan.ps1

Save this as aurora_port_scan.ps1:

Write-Host "`n=== AURORA SERVER DIAGNOSTIC REPORT ===`n" -ForegroundColor Cyan

# 1. Check all listening ports
Write-Host "▶ Checking Listening Ports..." -ForegroundColor Yellow
$ports = Get-NetTCPConnection -State Listen | Select-Object LocalPort,OwningProcess
$ports | Format-Table -AutoSize

# 2. Map PIDs to processes
Write-Host "`n▶ Mapping PIDs To Processes..." -ForegroundColor Yellow
foreach ($p in $ports) {
    try {
        $proc = Get-Process -Id $p.OwningProcess -ErrorAction SilentlyContinue
        if ($proc) {
            Write-Host "Port $($p.LocalPort) → $($proc.ProcessName) (PID $($p.OwningProcess))"
        }
    } catch {}
}

# 3. Check if Node/Express Backend is Running
Write-Host "`n▶ Checking for Express Backend (port 5000)..." -ForegroundColor Yellow
$express = $ports | Where-Object { $_.LocalPort -eq 5000 }
if ($express) {
    Write-Host "✔ Express backend is RUNNING on port 5000" -ForegroundColor Green
} else {
    Write-Host "✘ Express backend NOT detected on port 5000" -ForegroundColor Red
}

# 4. Check for Python AI backend (port 8000 / 8100 / 9000)
$aiPorts = @(8000,8100,9000)
foreach ($p in $aiPorts) {
    Write-Host "`n▶ Checking AI backend port $p..." -ForegroundColor Yellow
    $check = $ports | Where-Object { $_.LocalPort -eq $p }
    if ($check) {
        Write-Host "✔ Python AI backend active on port $p" -ForegroundColor Green
    } else {
        Write-Host "✘ No AI backend detected on port $p" -ForegroundColor Red
    }
}

# 5. Docker Containers
Write-Host "`n▶ Checking Docker Containers..." -ForegroundColor Yellow
docker ps


Run it with:

powershell -ExecutionPolicy Bypass -File aurora_port_scan.ps1

✅ 2. Linux/macOS Shell Script — aurora_port_scan.sh
#!/bin/bash
echo -e "\n=== AURORA SERVER DIAGNOSTIC REPORT ===\n"

echo "▶ Checking Listening Ports..."
sudo lsof -i -P -n | grep LISTEN

echo -e "\n▶ Checking Express Backend (5000)..."
if sudo lsof -i:5000 -sTCP:LISTEN >/dev/null; then
    echo "✔ Express backend running!"
else
    echo "✘ Express backend NOT running."
fi

for P in 8000 8100 9000; do
    echo -e "\n▶ Checking Python AI backend on $P..."
    if sudo lsof -i:$P -sTCP:LISTEN >/dev/null; then
        echo "✔ AI backend detected on port $P"
    else
        echo "✘ No AI backend on port $P"
    fi
done

echo -e "\n▶ Checking Docker containers..."
docker ps


Make executable:

chmod +x aurora_port_scan.sh


Run:

./aurora_port_scan.sh

🧠 What This Script Will Tell You

After running it, you’ll know:

What You Learn	Why It Matters
Which ports are active	Stops future conflicts
Whether Express is actually running	Confirms if port 5000 is free or in use
Whether any Python backend exists	Determines if old AI services were running
Whether Docker is exposing ports	Helps avoid double-binding errors
Which process owns a port	Essential for cleaning up ghost servers

This eliminates ALL confusion you’ve had about Aurora’s servers.