line-length = 100
target-version = "py311"

exclude = [
    "attached_assets",
    "backups",
    "runs",
    ".git",
    "__pycache__",
    "*.egg-info",
]

[lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = ["E203"]